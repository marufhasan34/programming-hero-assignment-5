1. 1.1 var is a Function-scoped variable that can be re-declared and updated.(a global scope variable)
   1.2 let variable cannot be re-declared but can be updated (a block scope variable)
   1.3 cont variable cannot be re-declared or updated (a block scope variable)

2. the spreed operator expands elements of an array or object

3. 3.1 map Creates a new array by transforming each element of the original array.

   3.2 filter Creates a new array with elements that pass a condition.

   3.3 forEach Loops through the array but does not return a new array.

4. an arrow function is a shorter syntax for writing functions in javascript

5. Template literals is a JavaScript feature that lets you create strings using backticks and embed variables
